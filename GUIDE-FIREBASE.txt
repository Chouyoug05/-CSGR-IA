═══════════════════════════════════════════════════════════════
  GUIDE RAPIDE : CONNEXION FIREBASE - ESPACE MEMBRE
═══════════════════════════════════════════════════════════════

ÉTAPE 1 : CONFIGURER FIRESTORE
────────────────────────────────
1. Allez sur https://console.firebase.google.com
2. Sélectionnez le projet "iagabon"
3. Firestore Database > Créer une base de données
4. Mode test > Activer

ÉTAPE 2 : RÈGLES FIRESTORE (IMPORTANT !)
─────────────────────────────────────────
Firestore Database > Règles > Coller EXACTEMENT :

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

⚠️ ATTENTION :
- Cliquez sur "Publier" après avoir collé les règles
- Attendez 30-60 secondes pour la propagation
- Les règles doivent être EXACTEMENT comme ci-dessus
- Si vous avez des erreurs de permissions, c'est que les règles ne sont pas correctes

ÉTAPE 3 : CRÉER LES COLLECTIONS AUTOMATIQUEMENT
───────────────────────────────────────────────
Les collections sont créées AUTOMATIQUEMENT au chargement de membre.html

1. Ouvrez membre.html dans votre navigateur
2. Ouvrez la console (F12)
3. Attendez 5-10 secondes
4. Vous devriez voir dans la console :
   - "🔄 Initialisation automatique des collections Firebase..."
   - "✅ Collection 'users' initialisée"
   - "✅ Collection 'statistiques' initialisée..."
   - "✅ Toutes les collections sont initialisées !"

Si vous voyez des erreurs de permissions :
- Vérifiez que les règles Firestore sont bien publiées (Étape 2)
- Attendez 30-60 secondes après avoir publié les règles
- Rechargez la page membre.html

Collections créées automatiquement :
- Collection "users" : 1 membre de test (username: membre1, password: membre123)
- Collection "statistiques" : 3 statistiques par défaut
- Collection "config" : documents cta, popup, contact
- Collections vides : programmes, actualites, inscriptions

ÉTAPE 4 : SE CONNECTER
───────────────────────
1. Sur la page membre.html
2. Username : membre1
3. Password : membre123
4. Cliquez sur "Se connecter"

═══════════════════════════════════════════════════════════════
  COLLECTIONS CRÉÉES
═══════════════════════════════════════════════════════════════

✅ users
   - Document avec username: "membre1", password: "membre123"

✅ programmes (vide)

✅ actualites (vide)

✅ statistiques
   - 3 statistiques par défaut

✅ inscriptions (vide)

✅ config
   - Document "cta"
   - Document "popup"
   - Document "contact"

═══════════════════════════════════════════════════════════════
  NOTES
═══════════════════════════════════════════════════════════════

- Le script initFirebaseCollections() peut être exécuté plusieurs fois
- Il ne crée que les collections/documents manquants
- Les données existantes ne sont pas écrasées
- Pour créer d'autres membres, utilisez la section "Membres" dans l'espace membre

═══════════════════════════════════════════════════════════════

